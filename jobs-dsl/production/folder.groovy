folder('demo-folder') {
    properties {
        folderLibraries {
            libraries {
                libraryConfiguration {
                    name("demo-lib")
                    defaultVersion("master")
                }
            }
        }
    }
}
